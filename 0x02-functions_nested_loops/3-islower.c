#include "holberton.h"

/**
 * _islower - This is a description
 * @c: The number to be checked
 * Return: The sum of the two parameters
 */
int _islower(int c)
{
	int c;

	if (c >= 97 && c <= 123)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
